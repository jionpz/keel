# OMP 模型可配 · 执行清单

1. [ ] `resolveModel(cli, env)` 纯函数 + 单测
2. [ ] `RunTaskOptions.model` → `new OmpAdapter({ model })`
3. [ ] `run-task` / `run-issue` argv `--model`；help 文案
4. [ ] `run-issue` options 透传
5. [ ] README / acceptance README 一行
6. [ ] `pnpm run check`

不跑五连（缺省未变）。可选：人工 `KEEL_MODEL=…` 单次 issue-e2e，不作为 AC。
